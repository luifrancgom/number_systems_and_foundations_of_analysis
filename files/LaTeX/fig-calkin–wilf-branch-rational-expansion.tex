\documentclass{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning}

\begin{document}

\begin{tikzpicture}[
  level distance=1.5cm,
  level 1/.style={sibling distance=6.5cm},
  level 2/.style={sibling distance=1cm},
  level 3/.style={sibling distance=1cm},
  level 4/.style={sibling distance=1cm},
  level 5/.style={sibling distance=1cm},
  level 6/.style={sibling distance=1cm},
  every node/.style={align=center}
]

\node {$x$}
  child {
    node {$\frac{x}{x+1}$}
      child[missing]
      child {
        node {$\frac{x}{x+1} + 1$}
          child[missing]
          child {
            node {$\frac{x}{x+1} + 2$}
              child[missing]
              child {
                node {$\frac{x}{x+1} + 3$}
                  child[missing]
                  child {
                    node[rotate=20] {$\vdots$}
                      child[missing]
                      child {
                        node {$\frac{x}{x+1} + k$}
                      }
                  }
              }
          }
      }
  }
  child {
    node {$x + 1$}
      child {
        node {$\frac{x+1}{x+2}$}
          child {
            node {$\frac{x+1}{2x+3}$}
              child {
                node {$\frac{x+1}{3x+4}$}
                  child {
                    node[rotate=-20] {$\vdots$}
                      child {
                        node {$\frac{x+1}{kx + (k+1)}$}
                      }
                      child[missing]
                  }
                  child[missing]
              }
              child[missing]
          }
          child[missing]
      }
      child[missing]
  };
\end{tikzpicture}

\end{document}